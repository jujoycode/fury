import { CLI_CONFIRM, CLI_INPUT, CLI_SELECT } from '../interface/cli';
export declare const CONFIG: {
    PROJECT_NAME: CLI_INPUT;
    PACKAGE_MANAGER: CLI_SELECT;
    PROJECT_LANG: CLI_SELECT;
    FRAMEWORK_USAGE: CLI_CONFIRM;
    PROJECT_TEMPLATE: CLI_SELECT;
    GIT_USAGE: CLI_CONFIRM;
    GIT_REPOSITORY_URL: CLI_INPUT;
    COMMIT_TYPE: CLI_SELECT;
    COMMIT_MESSAGE: CLI_INPUT;
    PUSH_PERMISION: CLI_CONFIRM;
};
